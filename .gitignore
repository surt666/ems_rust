# OCaml build
_build
.worktrees

# Lambda artifact
*.zip
bootstrap

# CDK output (any infra/*/cdk.out, plus root if anyone runs cdk from here)
cdk.out/
.cdk.staging/

# Playwright MCP screenshots / traces
.playwright-mcp/

# Local agent state
.claude/

# Stray screenshots dropped at the project root by Playwright
/*.png

__pycache__

# Go aggregations lambda build artifacts
infra/daq/data_pipeline/lambda/aggregations-go/bootstrap
infra/daq/data_pipeline/lambda/aggregations-go/aggregations-go.zip

infra/hierarchy/lambda/cognito-sync/bootstrap
infra/hierarchy/lambda/cognito-sync/cognito-sync.zip

# Rust build artifacts
/target
crates/**/target

# superpowers brainstorm visual companion (local, not committed)
.superpowers/
